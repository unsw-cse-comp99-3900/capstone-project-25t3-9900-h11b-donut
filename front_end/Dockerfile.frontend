# Dockerfile.frontend
FROM node:20-alpine

WORKDIR /app

# 先拷贝 package.json / package-lock.json（如果你有）
COPY package*.json ./

# 安装依赖
RUN npm install

# 再拷贝剩下的前端代码
COPY . .

# Vite dev server 默认端口是 5173
EXPOSE 5173

# 启动 Vite 开发服务器，监听 0.0.0.0:5173
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5173"]
