# -----------------------------
# 🗄️ MySQL 数据库配置
# -----------------------------
# 初始化数据库名
MYSQL_DATABASE=ai_learning_coach

# 普通用户（供后端连接）
MYSQL_USER=demo

# 用户密码（建议团队成员在 .env 中改成自己的安全密码）
MYSQL_PASSWORD=demo

# root 管理员密码（用于 MySQL 初始化）
MYSQL_ROOT_PASSWORD=root


# -----------------------------
# ⚙️ Flask 后端配置
# -----------------------------
# Flask 应用监听端口（compose 会把宿主机同样映射到这个端口）
BACKEND_PORT=9900

# 学期代码（你 app.py 里有用到 SEMESTER_CODE）
SEMESTER_CODE=2025T1

# Flask 的密钥（用于 session、加密）
# 请在自己的 .env 中换成随机字符串，如 openssl rand -hex 32
FLASK_SECRET_KEY=please-change-me
